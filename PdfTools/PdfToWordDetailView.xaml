<UserControl
    x:Class="ConversionApp.PdfTools.PdfToWordDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ConversionApp.PdfTools">

    <Grid>
        <!-- Selection Phase -->
        <Grid x:Name="FileSelectionPhase" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel Spacing="28" HorizontalAlignment="Center" MaxWidth="640" Padding="40">
                <TextBlock Text="PDF to Word" Style="{StaticResource TitleTextBlockStyle}" HorizontalAlignment="Center"/>
                <Border x:Name="DropZone" Background="{ThemeResource AccentFillColorDefaultBrush}" CornerRadius="16" Padding="48,56" AllowDrop="True" DragOver="DropZone_DragOver" DragLeave="DropZone_DragLeave" Drop="DropZone_Drop" MinWidth="520" MinHeight="220">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
                        <FontIcon Glyph="&#xE8E5;" FontSize="48" Foreground="White" HorizontalAlignment="Center"/>
                        <Button Click="ChooseFileButton_Click" HorizontalAlignment="Center" Padding="24,10" Background="White" Foreground="{ThemeResource AccentFillColorDefaultBrush}">
                            <TextBlock Text="CHOOSE FILE" FontWeight="SemiBold"/>
                        </Button>
                        <TextBlock Text="or drop a .pdf file here" Foreground="White" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <!-- Converting Phase -->
        <Grid x:Name="ConvertingPhase" Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel Spacing="24" HorizontalAlignment="Center" MaxWidth="640" Padding="40">
                <TextBlock Text="PDF to Word" Style="{StaticResource TitleTextBlockStyle}" HorizontalAlignment="Center"/>
                
                <!-- File Card -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="10" Padding="20,16" MinWidth="480">
                    <Grid ColumnSpacing="14">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <FontIcon Glyph="&#xEA90;" FontSize="28" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock x:Name="SelectedFileName" FontWeight="SemiBold"/>
                            <TextBlock x:Name="SelectedFileSize" Style="{StaticResource CaptionTextBlockStyle}"/>
                        </StackPanel>
                        <Button Grid.Column="2" Click="RemoveFile_Click" Background="Transparent" BorderThickness="0"><FontIcon Glyph="&#xE711;"/></Button>
                    </Grid>
                </Border>

                <!-- Convert Button -->
                <Button Style="{StaticResource AccentButtonStyle}" Command="{x:Bind ViewModel.ConvertCommand}" HorizontalAlignment="Stretch" Padding="0,14" CornerRadius="8">
                    <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Center">
                        <TextBlock Text="Convert to Word" FontWeight="SemiBold"/>
                        <ProgressRing IsActive="{x:Bind ViewModel.IsConverting, Mode=OneWay}" Width="20" Height="20"/>
                    </StackPanel>
                </Button>

                <!-- Status Card -->
                <Border Visibility="{x:Bind ViewModel.IsStatusVisible, Mode=OneWay}" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16,12">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <FontIcon Glyph="&#xE73E;" Foreground="#4CAF50" Visibility="{x:Bind ViewModel.IsSuccess, Mode=OneWay}"/>
                        <FontIcon Glyph="&#xEA39;" Foreground="#F44336" Visibility="{x:Bind ViewModel.IsError, Mode=OneWay}"/>
                        <TextBlock Text="{x:Bind ViewModel.StatusText, Mode=OneWay}" TextWrapping="Wrap" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Success Actions -->
                <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Center" Visibility="{x:Bind ViewModel.IsSuccess, Mode=OneWay}">
                    <Button Command="{x:Bind ViewModel.OpenFileCommand}" Padding="16,8">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE8A5;" FontSize="14"/>
                            <TextBlock Text="Open File"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{x:Bind ViewModel.OpenFolderCommand}" Padding="16,8">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE8B7;" FontSize="14"/>
                            <TextBlock Text="Open Folder"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- Error Actions -->
                <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Center" Visibility="{x:Bind ViewModel.IsError, Mode=OneWay}">
                    <Button Command="{x:Bind ViewModel.CopyErrorCommand}" Padding="16,8">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE8C8;" FontSize="14"/>
                            <TextBlock Text="Copy Error Details"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <HyperlinkButton Content="Back to PDF Tools" Click="BackButton_Click" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
